import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.ecoschool.itcontrolcenter',
  appName: 'Eco IT Control Center',
  webDir: 'build'
};

export default config;
